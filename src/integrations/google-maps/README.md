# integrations/google-maps

Placeholder para o adapter de Google Maps / Places / Routes.

Nesta etapa o código continua em `src/components/maps/*` e dentro
das páginas de transporte (`V3RideRequest`, `PertoDeMim`, etc). A
extração acontece na Etapa 4 do plano de refatoração.
