/**
 * Barrel público — Discovery / Recommendations.
 * Apenas o service é exportado externamente. O repository é interno.
 */
export { discover, type DiscoverOptions } from "./services/discoveryService";
