# modules/partner/invitations

**Status:** placeholder — o produto de Convites ainda não tem serviço
próprio. O tipo `"invite"` existe em `@modules/partner/validator` como
stub ("ainda não disponível"). Quando o fluxo real for implementado,
esta pasta receberá `services/`, `repositories/` e `types/` seguindo
o mesmo padrão dos demais módulos Partner.

Adiado nesta onda para respeitar o limite de risco.
