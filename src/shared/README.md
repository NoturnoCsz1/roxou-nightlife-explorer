# shared

Componentes e utilitários compartilhados entre apps. Receberá `components/`
(ex-`src/components/` raiz, `maps/`, `partners/`, `season/`) na Fase 6.
`shared/ui/` permanecerá apontando para `src/components/ui/` (shadcn, intocado).
