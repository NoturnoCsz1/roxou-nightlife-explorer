/**
 * Constantes compartilhadas do EventoForm (Fase 3C2 — consolidação).
 * Mantém valores idênticos aos do `EventoForm.tsx` original.
 */

/** Classe Tailwind padrão de inputs/selects/textareas do formulário. */
export const INPUT_CLASS =
  "w-full rounded-lg border border-border/50 bg-background px-3 py-2 text-sm outline-none focus:border-primary/50 transition";
