/**
 * PartnerLoginPage — placeholder (Fase 9A).
 * Futuro: login Google, login email, solicitar acesso, recuperar acesso.
 */
const PartnerLoginPage = () => {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="max-w-md text-center space-y-2">
        <h1 className="text-2xl font-bold">Roxou Partner Pro</h1>
        <p className="text-muted-foreground">Login do parceiro (em construção).</p>
      </div>
    </main>
  );
};

export default PartnerLoginPage;
