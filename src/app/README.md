# src/app

Bootstrap da aplicação: providers, roteador raiz e composição de layouts
por domínio. **Ainda não populado** — nesta etapa a pasta apenas existe
para receber, em ondas futuras, `App.tsx`, `main.tsx` e o roteador
segmentado por módulo (`portal.routes.tsx`, `partner.routes.tsx`, etc).

Não mover arquivos para cá agora.
